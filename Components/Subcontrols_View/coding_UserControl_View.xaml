<UserControl x:Class="Data_Logger_1._3.Components.Subcontrols_View.coding_UserControl_View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Data_Logger_1._3.Components.Subcontrols_View" xmlns:components="clr-namespace:Data_Logger_1._3.Components"
             mc:Ignorable="d"
             d:Background="White"
             x:Name="control_CODING_fields_View" >

    <Grid Width="380">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="5" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="text_BUGS" Style="{DynamicResource text_Placeholder}"
                   Foreground="{DynamicResource BLACK}"
                   Height="35"
                   Margin="0 5"
                   Padding="10"
                   Text="{Binding}"/>

        <TextBlock Grid.Row="2" x:Name="text_IsLAUNCHSUCCESSFUL" Style="{DynamicResource text_Placeholder}"
                   Foreground="{DynamicResource BLACK}"
                   Height="35"
                   Margin="0 5"
                   Padding="10"
                   Text="{Binding}" />


    </Grid>

</UserControl>
